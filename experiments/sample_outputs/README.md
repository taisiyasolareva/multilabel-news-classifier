# Sample outputs for reviewers

These files let you open dashboards and explore results without retraining.

---

## Evaluation dashboard inputs

Upload one of:
- `experiments/sample_outputs/distilmbert_lora_10k_v1_val_preds_sample_50.csv` (50 val samples; lightweight)

---

## Model comparison inputs

The model comparison dashboard reads experiment outputs from:
- `experiments/results/` (metrics JSONs)


